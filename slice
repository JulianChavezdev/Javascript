// slice()
//"porciona" le decimos el numero por posiciones. desde donde empieza o desde dodne empieza y termina.
// para empezar al reves empezar con el ultimo se pueden usar valores negativos. (-x)
//slice no modifica el array original.

const animals = ['ant', 'bison', 'camel' ,'duck', 'elephant']
console.log(animals.slice(2))
console.log(animals.slice(2, 4))

